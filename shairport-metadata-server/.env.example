# Shairport Metadata Server Configuration
# Copy this file to .env and modify as needed

# MQTT Broker Settings
MQTT_HOST=localhost
MQTT_PORT=1883
# MQTT_USER=
# MQTT_PASS=

# Shairport Sync topic prefix (must match shairport-sync.conf)
TOPIC_PREFIX=shairport-sync

# HTTP Server Settings
HTTP_HOST=0.0.0.0
HTTP_PORT=5000
